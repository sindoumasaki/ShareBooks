<% if comment.comment_favorited_by?(current_user) %>
  <%= link_to "いいね削除", user_book_comment_comment_favorites_path(book.id, comment.id), method: :delete, class: "btn btn-danger" %>
<% else %>
   <%= link_to "いいね", user_book_comment_comment_favorites_path(book.id, comment.id), method: :post, class: "btn btn-success" %>
<% end %>