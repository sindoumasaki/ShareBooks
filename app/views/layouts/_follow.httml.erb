<%= form_with model: current_user, url: user_user_relationships_path, method: :post, remote: true do |f| %>
  <%= hidden_field_tag :following_id, user.id %>
  <%= f.submit "フォローする", class: "btn btn-outline-secondary" %>
<% end %>